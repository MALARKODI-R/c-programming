//program to add two integers
#include<stdio.h>
void main(){
  int a,b,c;
  printf("Enter two numbers: ");//the numbers should be in integer format
  scanf("%d %d",&a,&b);
  c=a+b;
  printf("The sum of given integer is %d",c);
  }
