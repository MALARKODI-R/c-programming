// Write a C program to perform a binary search in an array
#include <stdio.h>

int main() {
    int a[5]={1,2,3,4,9};
    for(int i=0;i<5;i++){
        if(a[i]==5){
            found=1;
            printf("%d is found",a[i]);
            break;
        }
    }

}
